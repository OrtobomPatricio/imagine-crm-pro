# =====================
# Imagine Lab CRM PRO
# =====================

# ==== Server ====
NODE_ENV=production
# Bind host (useful for Docker/local)
HOST=0.0.0.0
PORT=3000

# Ports used by docker-compose
APP_PORT=3000
MYSQL_PORT=3307

# MySQL connection string (mysql://USER:PASSWORD@HOST:PORT/DB)
# For Docker, use service name 'mysql' as host when running inside container, but for this env file shared with docker-compose, 
# usually we keep it compatible or override it in docker-compose.
# Default for Docker setup:
DATABASE_URL=mysql://crm:secure_password@mysql:3306/chin_crm

# ==== Auth (required) ====
# Generate a long random string
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# Owner principal (who provides the token for admin access)
OWNER_OPEN_ID=admin-owner

# ==== OAuth portal ====
VITE_OAUTH_PORTAL_URL=https://tu-dominio.com
VITE_APP_ID=chin-crm-prod
OAUTH_SERVER_URL=https://tu-dominio.com

# ==== DEV LOGIN (Disable in production usually) ====
ALLOW_DEV_LOGIN=0
VITE_DEV_BYPASS_AUTH=0

# ==== Secrets encryption ====
# 32+ chars required
DATA_ENCRYPTION_KEY=CHANGE_ME_32CHARS_MINIMUM_LENGTH_KEY

# ==== WhatsApp Cloud API ====
WHATSAPP_WEBHOOK_VERIFY_TOKEN=CHANGE_ME_TOKEN
WHATSAPP_APP_SECRET=
WHATSAPP_GRAPH_VERSION=v19.0
WHATSAPP_GRAPH_BASE_URL=https://graph.facebook.com

# ==== Optional: storage/notification proxy ====
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
