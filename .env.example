# =====================
# Imagine Lab CRM PRO
# =====================

# ==== Server ====
NODE_ENV=development
# Bind host (useful for Docker/local)
HOST=0.0.0.0
PORT=3000

# Ports used by docker-compose
APP_PORT=3000
MYSQL_PORT=3306

# MySQL connection string (mysql://USER:PASSWORD@HOST:PORT/DB)
# TIP (Windows + Docker): usá 127.0.0.1 en vez de localhost
DATABASE_URL=mysql://root:password@127.0.0.1:3306/chin_crm

# ==== Auth (required) ====
# Usá un secreto largo y random
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# Owner principal (quien tiene control total)
# Poné cualquier texto único, ejemplo: dev-owner
OWNER_OPEN_ID=dev-owner

# ==== OAuth portal (si usás login real con Google / OAuth) ====
VITE_OAUTH_PORTAL_URL=http://localhost:3000
VITE_APP_ID=chin-crm-local
OAUTH_SERVER_URL=http://localhost:3000

# ==== DEV LOGIN (recomendado para pruebas locales) ====
# Esto habilita el botón: "Entrar como DEV (sin Google)"
ALLOW_DEV_LOGIN=1
VITE_DEV_BYPASS_AUTH=1

# ==== Secrets encryption (required si vas a guardar tokens de WhatsApp / integraciones) ====
# 32+ chars recomendado (ej: openssl rand -hex 32)
DATA_ENCRYPTION_KEY=CHANGE_ME_32CHARS_MINIMUM

# ==== WhatsApp Cloud API (webhook + envio) ====
# Verificacion del webhook (Meta -> Webhooks -> Verify token)
WHATSAPP_WEBHOOK_VERIFY_TOKEN=CHANGE_ME

# App Secret de Meta (para validar firmas X-Hub-Signature-256)
WHATSAPP_APP_SECRET=

# Graph API
WHATSAPP_GRAPH_VERSION=v19.0
WHATSAPP_GRAPH_BASE_URL=https://graph.facebook.com

# ==== Optional: storage/notification proxy ====
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
